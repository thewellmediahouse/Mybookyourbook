/**
 * Single switch for Design Your Website studio.
 * - `false` (default): pages not built, nav hidden, APIs 404
 * - `true`: routes injected, nav shown, APIs live
 *
 * Pages live in `src/features/design-studio/pages/` (kept even when off).
 */
export const DESIGN_STUDIO_ENABLED = true;
